// src/ThemeContext.js
import { createContext } from 'react';

// Define a new context with a default value of 'light' and export it [cite: 28]
const ThemeContext = createContext('light');

export default ThemeContext;